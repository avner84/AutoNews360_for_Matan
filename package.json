{
  "description": "",
  "scripts": {
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && node app.js\" ",
    "install-all": "npm install && cd frontend && npm install && cd .. && cd backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
